#This is stuff we do in the terminal

#to create a new R code file
    #GENERAL TEMPLATE:
        #usethis::use_r("file_name")

# usethis::use_r("test")

###### IN TERMINAL FOR GIT
# The sequence is four steps

      #
      # STEP 1
      # mkdir
          #only if you don't have a local folder
      # STEP 2
      # git init
      # STEP 3
      # git remote add origin
      # STEP 4
      # git remote -v

#STEP 1
#to create a new folder

                # $mk dir

#STEP 2
#Git init says - this local folder I care about it, I want to make an empty git repository there


#STEP 3
#git remote add origin - adds name of local repository


#STEP 4
#git remote -v
  # says - what remote folder are you looking at, git repository?
